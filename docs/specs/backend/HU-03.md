# HU-03 (backend) — Autenticación Auth0 end-to-end

> **Jira:** `TS-27` (manejador de excepciones) · `TS-28` (middleware JWT + rol) · `TS-29` (endpoint de humo `/api/v1/me`) · **Sprint:** 1 · **RF/RNF que cubre:** RNF-01 (control de acceso), RNF-02 (confidencialidad)
> **Contraparte:** `../frontend/HU-03.md` — **aún no escrita**. El frontend ya tiene `Auth0Provider` y `useRole` implementados (rama `feature/TS-18-frontend-Auth0-login`); la spec de esa capa está en deuda.
> **Sigue en:** [`HU-04.md`](HU-04.md) — RBAC y el 403. Esta HU deja el rol disponible; HU-04 lo usa para autorizar.
> **Estado:** `[x]` borrador · `[x]` **aprobada** (2026-09-17) · `[ ]` implementada

Estas tres tareas son los **cimientos** del backend: ninguna línea de dominio se escribe hasta que un error tiene forma estable y un request tiene identidad. TS-27 no depende de Auth0 y va primero.

---

## 1. Alcance en esta capa

**Dentro:**

- **TS-27** — Manejador de excepciones centralizado en `bootstrap/app.php → withExceptions` que emite el cuerpo de **D3.1** para todo error de la API, con el catálogo de códigos como enum nativo (D4.6). Incluye el enrutado `/api/v1` (D3.1 del bloque 3, punto 3.1) porque el manejador solo aplica a peticiones de API.
- **TS-28** — Validación del JWT de Auth0 y **extracción del rol** desde el claim namespaced de D4.8, disponible para las Policies de HU-04.
- **TS-29** — `GET /api/v1/me`: endpoint de humo autenticado que devuelve la identidad del token vía API Resource (D4.2), incluido el rol.

**Fuera / diferido (con motivo):**

- **Policies y el 403 del artista** → HU-04 (TS-3x). Aquí el rol solo se **extrae**; no se autoriza con él todavía.
- **Migraciones de `users`, `artists`, `production_access`** → T-31, dentro de HU-04. **Consecuencia directa:** `/api/v1/me` de TS-29 **no toca la base de datos** — sirve la identidad del token, no una fila local. Ver §3.4.
- **Provisión JIT del usuario local** (resolver `users` por `auth0_sub`) → HU-04, que es donde nace la tabla. Ya está anotado como pendiente en [`HU-04.md`](HU-04.md) §5.
- **`GET /api/v1/health`** (D9.3, sin autenticación, verifica la BD) → es de HU-02/observabilidad, no de estas tres tareas. No se cuela aquí aunque sea una ruta cómoda para probar.
- **Códigos del catálogo de D3.1 ligados a audio/S3** (`UNSUPPORTED_MEDIA_TYPE`, `PAYLOAD_TOO_LARGE`, `AUDIO_HASH_MISMATCH`, `PRESIGNED_URL_EXPIRED`): se **declaran** en el enum porque el catálogo es una unidad, pero **no se cablean** a ninguna excepción — no hay nada que los dispare y "Qué NO hacer todavía" del `CLAUDE.md` veta tocar S3.
- **`RATE_LIMITED` (429)**: se declara y se cablea (es una línea: `ThrottleRequestsException`), pero la política de rate limiting en sí es **8.3 del ADR, ABIERTO**. No se configura ningún limitador.

## 2. Dependencias

- **ADR (DECIDIDO):** `3.1` (prefijo `/api/v1`), **`D3.1`** (formato de error + catálogo), `D4.2` (API Resources), `D4.4` (capas), `D4.6` (enums backed), **`D4.8`** (rol desde el claim), `D9.3` (Sentry como consumidor del `trace_id`), `10.2` (cuenta de Auth0 **HECHA**, confirmada 2026-08-19).
- **ADR movido en esta sesión:** **`3.6` → `DECIDIDO`** (RFC 9457 completo) y **`3.7`**, que sigue `ABIERTO` pero **ya no bloquea** — la implementación es compatible con sus dos salidas posibles (ver §3.1). Los dos estaban frenando TS-27 por la Definition of Ready (criterio 4). El cambio vive en el ADR; **esta spec no es la fuente de verdad de esa decisión**, solo la consume.
- **Tareas previas:** T-20 a T-23 (tenant, API, app SPA, roles y Action post-login; claim verificado en un token real) — **confirmadas hechas**. T-06 (scaffolding) — hecho.
- **Externas:** dominio `trackstudio.site` confirmado, por lo que el namespace del claim y las URLs de `type` son definitivos, no placeholders.
- **Paquete a instalar:** `auth0/login:^7` (namespace `Auth0\Laravel`). **No** la beta de la v8, **no** la "v4.x" que menciona D4.8 — esa cifra es un error del documento, ya registrado en el handoff de backend §8.1.

## 3. Contrato técnico

Flujo de request de D4.8: `Middleware(JWT/claim) → Form Request → Controller → Policy → Service → Eloquent/Contratos → API Resource → JSON`. Estas tres tareas construyen el primer eslabón y el último.

### 3.1 Enrutado y forma del error (TS-27)

**Enrutado.** `withRouting(api: routes/api.php, apiPrefix: 'api/v1')` en `bootstrap/app.php`. El prefijo `api/v1` sale de 3.1; el SDK de Auth0 registra su guard sobre el grupo `api`, así que las rutas **deben** vivir en `routes/api.php` (requisito del propio SDK, no nuestro).

**Cuerpo del error.** El de **D3.1**, en su variante **completa de RFC 9457** (decisión 3.6, cerrada en esta sesión): todos los campos del ejemplo del ADR, incluidos `type` e `instance`.

| Campo      | Origen                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------ |
| `type`     | `https://trackstudio.site/errors/{code en kebab-case}` — derivado del `code`, no escrito a mano |
| `title`    | Frase en español por código (nomenclatura §1: texto de cara al usuario en español)          |
| `status`   | El HTTP de la respuesta; siempre coincide con él                                            |
| `code`     | El del catálogo de D3.1, en `SCREAMING_SNAKE_CASE`                                          |
| `detail`   | Mensaje concreto del caso; en 500 con `app.debug=false`, **nunca** el `getMessage()` real    |
| `instance` | La ruta pedida (`$request->getPathInfo()`)                                                  |
| `errors`   | **Solo en 422**; forma nativa de Laravel (`campo → [mensajes]`)                             |
| `trace_id` | ULID generado por el backend en cada respuesta de error (decisión 3.7, ver abajo)           |

> **Drift del ADR, ya corregido (2026-09-17):** el ejemplo de D3.1 usaba `https://trackstudio.app/errors/validation`, resto de antes de comprar el dominio. Ahora dice **`trackstudio.site`**, el mismo del namespace del claim de D4.8. Se anota aquí porque el test 2 de §4 afirma el dominio: si alguien lo revierte, se pone rojo.

**Decisión 3.7 — origen del `trace_id`:** lo genera el **backend** como ULID (`Str::ulid()`, nativo de Laravel) cuando construye la respuesta de error. Si más adelante el frontend manda `X-Trace-Id`, el manejador lo respeta y solo genera cuando falta — un cambio de una línea en un solo sitio. Los tests de §4 afirman **presencia y forma** del `trace_id`, no su origen, así que no bloquean esa evolución.

**Catálogo como enum** (D4.6). Un `enum ErrorCode: string` que conoce su `status`, su `title` y deriva su `type`.

> **Excepción de nomenclatura, deliberada:** D4.6 y `nomenclatura.md` §6 piden el valor backed en minúscula/`snake_case`. Aquí el valor backed **es** el `code` del contrato con el frontend, que D3.1 fija en `SCREAMING_SNAKE_CASE`. Manda D3.1: el valor sale por el cable. Los **casos** del enum siguen en `PascalCase` (`case ResourceNotFound = 'RESOURCE_NOT_FOUND';`).

**Mapeo excepción → código.** Lo que el manejador cablea en esta tarea:

| Excepción                                              | HTTP | `code`                |
| ------------------------------------------------------ | ---- | --------------------- |
| `ValidationException`                                  | 422  | `VALIDATION_ERROR`    |
| `AuthenticationException`                              | 401  | `UNAUTHENTICATED`     |
| `AuthorizationException` · `AccessDeniedHttpException` | 403  | `FORBIDDEN`           |
| `NotFoundHttpException` · `ModelNotFoundException`     | 404  | `RESOURCE_NOT_FOUND`  |
| `ThrottleRequestsException`                            | 429  | `RATE_LIMITED`        |
| Cualquier otra                                         | 500  | `INTERNAL_ERROR`      |

**Regla de seguridad (D3.1).** Con `app.debug=false`, el `detail` de un 500 es un texto genérico fijo; el mensaje real va al log/Sentry ligado al `trace_id`. Con `app.debug=true` sí puede exponerse — es lo que hace útil el entorno local, y lo que hace que el test 5 de §4 pueda ponerse rojo.

### 3.2 Validación del JWT y extracción del rol (TS-28)

**Quién valida el JWT: el SDK, no nosotros.** Firma, JWKS, `iss`, `aud` y expiración los resuelve `auth0/login` v7 a través de su guard `auth0-api` (driver `auth0.authorizer`), aplicado con el middleware `auth:auth0-api` de Laravel. Escribir esa validación a mano es el error que el handoff de backend §8.3 marca explícitamente.

> **Decisión de diseño — aprobada el 2026-09-17.** El sprint doc (T-25) y D4.8 dicen "un middleware valida el JWT y extrae el claim". La primera mitad ya la cumple el guard del SDK. Para la segunda, **no hace falta una clase de middleware propia**: el punto de extensión que el SDK expone para esto es `UserRepositoryContract::fromAccessToken(array $user)`, que recibe los claims ya validados. Poner ahí la lectura del rol lo hace imposible de saltar (todo request autenticado pasa por él), mientras que un middleware aparte hay que acordarse de registrar en cada ruta. Se cumple D4.8 con el guard + el repositorio, y **no** se escribe una clase `Middleware` que solo reenvíe. D4.8 quedó reescrito en el ADR para decir esto mismo, así que el documento y el código no divergen. **Consecuencia para la trazabilidad:** el título de TS-28 en Jira ("Middleware que valida el JWT…") describe el efecto, no el artefacto; el artefacto es `App\Auth\UserRepository`. Anotarlo en el PR para que el tribunal no busque una clase que no existe.

**Qué se escribe en TS-28:**

- `App\Auth\UserRepository implements Auth0\Laravel\UserRepositoryContract` — `fromAccessToken(array $user)` construye el usuario del request y le adjunta el rol leído del claim. `fromSession()` no aplica (arquitectura SPA + API, guard stateless; el handoff §8.2 descarta el flujo de sesión) y lanza/devuelve nulo explícitamente en vez de quedarse vacío por accidente.
- `enum Role: string` (D4.6) con exactamente dos casos: `case Productor = 'productor';` y `case Artista = 'artista';`. Los **valores** son los de Auth0 (D4.8) y están en español a propósito: son datos, no identificadores (nomenclatura §1).
- **Una sola clave de config** para el namespace: `config('auth0.roles_claim')`, alimentada por `AUTH0_ROLES_CLAIM` en `.env`. Es el punto que el handoff §8.8 exige centralizar, porque el mismo literal vive en la Action de Auth0 y en `VITE_AUTH0_ROLE_CLAIM` del frontend. **Ningún otro archivo del backend escribe ese literal**, y hay un test que lo prueba (§4, test 12).
- `config/auth.php`: el guard `auth0-api` apuntando al provider con este repositorio. `.env.example` con las claves `AUTH0_*` **sin valores reales** (D2.2, repo público).

**Contrato del claim.** El claim llega como **array de strings** (`"https://trackstudio.site/roles": ["artista"]`), que es la forma que produce la Action de Auth0. Se toma el primer valor; si el claim falta o viene vacío, el rol es `null` y **no se lanza excepción**: un token válido sin rol es un usuario autenticado sin permisos, es decir 401 no, 403 sí — y ese 403 lo decide la Policy de HU-04, no este código. Un valor que no corresponda a ningún caso del enum se trata igual que ausente (rol `null`), nunca se propaga como string crudo.

### 3.3 Respuesta 401 (TS-28 × TS-27)

Sin token, o con token inválido, la petición a una ruta con `auth:auth0-api` termina en **401 con el cuerpo de D3.1 y `code: UNAUTHENTICATED`**. Esto **no** es gratis: hay que sobreescribir el `unauthenticated()` de Laravel / mapear la excepción en el manejador de TS-27, porque el 401 que emite el SDK por defecto no tiene esta forma (handoff §8.4). Es el punto de encuentro de las dos tareas y donde el `code` deja de ser una promesa del documento para ser una aserción de test.

### 3.4 `GET /api/v1/me` (TS-29)

- **Ruta:** `GET /api/v1/me`, middleware `auth:auth0-api`. Controller delgado (D4.4) → `UserResource` (D4.2). Sin Service: no hay lógica de negocio que orquestar, y un Service que solo reenvía es la abstracción de un solo uso que el review caza (metodología §5).
- **Fuente de los datos: el token, no la base de datos.** Es deliberado y acotado a Sprint 1: la tabla `users` nace en T-31 (HU-04). Este endpoint prueba que la cadena *token → claim → rol → JSON* funciona end-to-end; conectarlo a una fila local es trabajo de HU-04, y se anota en §6 cuando ocurra.
- **Forma de la respuesta (200):**

```json
{ "data": { "sub": "auth0|…", "role": "productor" } }
```

> **Corregido el 2026-09-17 al escribir los tests.** Esta spec pedía además `name` y `email`. Un **access token** de Auth0 no los lleva: son claims del *ID token*. Traerlos exigiría o bien inyectarlos como claims custom en la Action post-login (T-22, trabajo de infraestructura que no está en estas tres tareas), o bien llamar a la Management API en cada request (una dependencia de red y un límite de tasa dentro de un endpoint de humo). Y no hacen falta: el SPA ya tiene nombre y correo del ID token vía `useAuth0().user`. El endpoint prueba la cadena *token → claim → rol*, que es su razón de ser. **Si se quieren de todas formas**, es una línea en la Action de Auth0 más actualizar esta spec y el test 16 — en ese orden.

- **El Resource filtra (D4.2).** Solo esos dos campos salen. Los claims de infraestructura del token (`iss`, `aud`, `exp`, `iat`, `azp`, `scope`, y `email` si algún día llega) **no** se devuelven: no le sirven al cliente y describen la configuración interna de auth. Hay un test que lo afirma (§4, test 17).
- `role` sale como el **valor** del enum (`"productor"` / `"artista"`) o `null` si el token no trae claim. Ese string es el mismo contrato que ya consume `useRole.ts` en el frontend.

## 4. Criterios de aceptación de esta capa → tests

> **Reescrita el 2026-09-17**, después de ver los tests en rojo. La numeración original (1-19) se conserva para poder seguir el historial; qué cambió y por qué está en §6.

Pest (D4.7). **Los tests afirman la forma del JSON, no solo el status** — uno que solo mira `status === 401` pasa aunque el cuerpo esté mal, que es el tipo de test que la metodología prohíbe. Los códigos se afirman con **literales**, nunca importando el enum `ErrorCode`: si el test leyera el valor del mismo sitio que el código, un valor mal escrito quedaría verde y rompería el contrato con el frontend en silencio.

**Helpers compartidos** en `tests/Pest.php`: `impersonarToken(array $claims)` y `claimsDeToken(?string $rol, array $extra)`. `claimsDeToken()` **lee el namespace de `config('auth0.roles_claim')`** en vez de escribirlo literal: un test que copia el literal deja de detectar que el código lo tiene incrustado.

**Sobre la frontera externa (C4).** `impersonarToken()` se salta la verificación criptográfica de Auth0 —firma, JWKS, `iss`, `aud`, expiración—, que no es nuestra para probar y que se verificó fuera de la suite contra un token real en jwt.io (T-23). Lo que **no** se salta es nuestro código: construye el usuario pasando los claims por `UserRepository::fromAccessToken()`.

### TS-27 — `tests/Feature/ErrorShapeTest.php`

Registran rutas desechables bajo el grupo `api`: lo que se prueba es el manejador, no un recurso de negocio que todavía no existe.

| #   | Test (caso)                                                       | Qué afirma                                                                                                                      | Cómo se prueba que FALLA (C3)                                                                              |
| --- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 1   | `devuelve el cuerpo completo de D3.1 ante un recurso inexistente` | 404 con el conjunto **exacto** de claves de D3.1 (`type`, `title`, `status`, `code`, `detail`, `instance`, `trace_id`) y `code === "RESOURCE_NOT_FOUND"`. | Quitar un campo del manejador, o añadir uno de más → rojo por el conjunto exacto.                           |
| 2   | `deriva el type del code contra el dominio del proyecto`          | `type === "https://trackstudio.site/errors/resource-not-found"`.                                                                | Revertir el dominio a `trackstudio.app` (el literal que tenía el ADR) → rojo.                              |
| 3   | `refleja en instance la ruta pedida`                              | `instance` es la ruta del request; el test pide dos rutas distintas y compara.                                                   | Devolver `instance` fijo → rojo por la segunda ruta.                                                       |
| 4   | `acompaña cada error con un trace_id con forma de ULID`           | `trace_id` presente, 26 caracteres Crockford base32, y **distinto entre dos peticiones**.                                        | Devolver un `trace_id` constante → rojo por la segunda aserción.                                           |
| 5   | `no filtra el mensaje interno de un 500 cuando debug está apagado` | Con `app.debug=false`, el mensaje de la excepción **no aparece en ningún campo** del cuerpo; `code === "INTERNAL_ERROR"`.        | Poner `$e->getMessage()` en `detail` sin mirar `app.debug` → rojo. **Aserción de RNF-02.**                  |
| 6   | `sí expone el mensaje cuando debug está encendido`                | Con `app.debug=true`, el mensaje **sí** está en `detail`. Ancla del 5.                                                          | Ocultar siempre el mensaje → rojo. Sin este test, el 5 pasaría aunque `detail` estuviera vacío siempre.     |
| 7   | `conserva la forma nativa de Laravel en los errores de validación` | 422, `code === "VALIDATION_ERROR"`, y `errors` con la forma `campo → [mensajes]`.                                               | Aplanar `errors` a un array de strings → rojo.                                                             |
| 9   | `traduce una excepción de autorización a 403 FORBIDDEN`           | `AuthorizationException` → 403, `code === "FORBIDDEN"`, `type` derivado.                                                        | Quitar esa rama del mapeo: cae en el 500 genérico → rojo.                                                  |
| 10  | `deja pasar las respuestas que no son de API`                     | Una ruta fuera del grupo `api` que falla devuelve HTML, no el JSON de D3.1.                                                     | Aplicar el formato a todo request → rojo. **⚠️ Nace en VERDE** — ver la nota de abajo.                      |

> **El test 8 desapareció.** Afirmaba "`errors` solo en las respuestas 422". Como test propio **nacía verde** (hoy un 404 tampoco trae `errors`), y la metodología prohíbe un test que no puede fallar. Su intención vive ahora dentro del test 1, que afirma el conjunto **exacto** de claves: si `errors` se colara en un 404, el 1 se pone rojo.
>
> **El test 10 nace verde y se queda.** Es un guardarraíl contra la sobre-aplicación del manejador, no un criterio de aceptación: no existe una implementación futura que lo vuelva rojo "por fin", su trabajo es ponerse rojo el día que alguien extienda el formato a todo el sitio. Queda anotado aquí y con un comentario en el archivo para que nadie lo lea como un descuido. **Es la única excepción de la suite.**

### TS-28 — `tests/Unit/UserRepositoryTest.php`

La extracción del rol es una función pura de los claims: se prueba directamente sobre `UserRepository::fromAccessToken()`, sin HTTP de por medio.

| #   | Test (caso)                                                  | Qué afirma                                                                                       | Cómo se prueba que FALLA (C3)                                                          |
| --- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 13  | `extrae el rol artista del claim del token`                  | Claim `["artista"]` → `Role::Artista`, el **enum**, no el string crudo.                            | Devolver el string sin castear → rojo por tipo.                                         |
| 13b | `extrae el rol productor del claim del token`                | Lo mismo para `productor`. Impide "arreglar" el 13 devolviendo siempre `Role::Artista`.            | Quemar un rol fijo → uno de los dos se pone rojo.                                       |
| 12  | `lee el namespace del claim desde una única clave de config` | Cambiando `auth0.roles_claim` en runtime, el rol se sigue leyendo bien.                            | Incrustar el literal del namespace en el repositorio → rojo. **Anti-drift, handoff §8.8.** |
| 14  | `deja el rol en null cuando el token no trae el claim`       | Token válido sin claim → `role === null`, **sin excepción**.                                       | Lanzar excepción al faltar el claim → rojo.                                             |
| 15  | `ignora un valor de rol que no está en el enum`              | Claim `["admin"]` → `role === null`, nunca `"admin"`.                                              | Usar `Role::from()` sin cubrir el fallo, o dejar pasar el string → rojo.                 |
| 15b | `conserva el sub del token como identificador del usuario`   | `getAuthIdentifier()` devuelve el `sub`. Es la clave con la que HU-04 resolverá `auth0_sub`.       | Devolver otro claim como identificador → rojo.                                          |

### TS-28 — `tests/Feature/TokenGuardTest.php`

Lo que la prueba unitaria no puede afirmar. Usa su **propia ruta protegida desechable**, no `/api/v1/me`: si usara la de TS-29, estos tests fallarían por una ruta que falta y no por el guard.

| #   | Test (caso)                                                              | Qué afirma                                                                                   | Cómo se prueba que FALLA (C3)                                                               |
| --- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 11  | `rechaza con 401 UNAUTHENTICATED la petición sin token`                  | Sin `Authorization` → 401 **y** cuerpo D3.1 con `code` y `type`.                               | Quitar la sobreescritura de `unauthenticated()`: el status sigue 401, el cuerpo pierde la forma → rojo. |
| 11b | `rechaza con 401 UNAUTHENTICATED un Authorization header con basura`     | Un token que no es un JWT → 401 con la misma forma, no un 500.                                 | Dejar que la excepción del SDK suba sin mapear → 500 → rojo.                                     |
| 11c | `usa nuestro repositorio como proveedor de usuarios del guard auth0-api` | `config('auth.providers.….repository')` es `App\Auth\UserRepository`.                         | Dejar el repositorio por defecto del SDK en `config/auth.php` → rojo.                            |

> **Por qué hizo falta el 11c.** `setImpersonating()` del SDK **pone el usuario directamente en el guard**: no pasa por `fromAccessToken()`. Es decir, ningún test que impersone puede demostrar que nuestro repositorio esté cableado — se descubrió al escribir el rojo. Sin el 11c, la extracción del rol podría estar perfecta y no ejecutarse nunca: el guard serviría el usuario por defecto del SDK, sin rol, y **todas las Policies de HU-04 negarían a todo el mundo con un 403 inexplicable**. Es una aserción de cableado, no de comportamiento; su prueba de comportamiento real es el primer login del frontend contra el backend desplegado.

### TS-29 — `tests/Feature/MeEndpointTest.php`

| #   | Test (caso)                                            | Qué afirma                                                                                                  | Cómo se prueba que FALLA (C3)                                                 |
| --- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 16  | `devuelve la identidad del token con su rol`           | 200 y `assertExactJson` de `data` con **solo** `sub` y `role`; `role === "productor"` (el literal del cable). | Añadir un campo de más al Resource → rojo por el JSON exacto.                    |
| 18  | `refleja el rol artista cuando el token es de un artista` | Mismo endpoint, token de artista → `role === "artista"`. Ancla del 16.                                      | Devolver `'productor'` fijo → rojo.                                              |
| 18b | `devuelve el rol en null cuando el token no trae el claim` | Token sin rol → 200 con `role: null`. El endpoint de humo no es quien rechaza.                             | Lanzar, o devolver `""` en vez de `null` → rojo.                                  |
| 19  | `exige token para responder`                           | `GET /api/v1/me` sin token → 401 `UNAUTHENTICATED`. El 11 aplicado a la ruta real.                           | Quitar `auth:auth0-api` de la ruta → 200 sin token → rojo.                       |
| 17  | `no expone los claims internos del token`              | Con un token "gordo" (`iss`, `aud`, `azp`, `scope`, `email`), **ninguno** aparece en la respuesta.            | Volcar `getAttributes()` en el Resource → rojo. **Aserción de RNF-02.**          |

## 5. Notas de implementación (para el humano)

**Orden.** TS-27 completo y en verde antes de tocar Auth0. Es la única de las tres que no depende de nada externo, y las otras dos se apoyan en su forma de error.

**TS-27:**

- El `withExceptions` ya tiene el `shouldRenderJsonWhen` del esqueleto: se conserva, es justo la condición del test 10.
- La derivación `type` ← `code` se escribe **una vez** dentro del enum (`Str::kebab(strtolower($this->value))` o equivalente). Si aparece en dos sitios, es el drift de §0 de la metodología esperando a ocurrir.
- El `detail` genérico del 500 también es texto de cara al usuario: en español.

**TS-28:**

- `sail composer require auth0/login:^7` y `sail artisan vendor:publish --tag auth0`. Recuerda que `config.platform.php` en `composer.json` está pinneado a `8.4.24`; si el contenedor subió de parche, Composer falla la resolución antes que nada (handoff §4.3).
- La clave `roles_claim` no viene en el `config/auth0.php` que publica el SDK: se añade a mano y se alimenta de `AUTH0_ROLES_CLAIM`.
- **Trampa:** las rutas tienen que estar en `routes/api.php` para que el guard del SDK se registre solo. En `routes/web.php` no funciona y el síntoma es un 401 que parece un problema de token.
- Antes de escribir el repositorio: `graphify query "user repository auth0"` — no hay nada todavía, pero la consulta es la que evita duplicar cuando HU-04 añada la versión con base de datos.

**TS-29:**

- Controller delgado y ya: `MeController::__invoke()` devolviendo `new UserResource(auth()->user())`. Si te encuentras escribiendo un `MeService`, para — no hay negocio que meter ahí (§3.4).
- El `UserResource` recibe un `StatelessUser` del SDK, no un modelo Eloquent. Cuando HU-04 traiga la tabla `users`, este Resource es el punto que habrá que revisar: anótalo en §6 cuando pase.

**Cierre (las tres):** `sail pint --test` → `sail php vendor/bin/phpstan analyse` → `sail pest`, en ese orden (backend `CLAUDE.md` §2), y cada test nuevo probado en rojo según la columna C3 de §4 antes de pedir el bloque de commit.

## 6. Registro de cambios de la spec

- `2026-09-17` — creada y **aprobada** el mismo día. Lo que se movió fuera de la spec al aprobarla:
  - **ADR `3.6` → `DECIDIDO`**: RFC 9457 completo, con `type` derivado del `code` sobre `trackstudio.site`.
  - **ADR `3.7`**: sigue `ABIERTO`, anotado como **no bloqueante** (el backend genera un ULID; si el frontend aporta `X-Trace-Id`, el manejador lo respeta). La metodología §8 recoge ahora esta distinción entre "abierto" y "bloqueante".
  - **Drift corregido en D3.1**: `trackstudio.app` → `trackstudio.site`.
  - **Drift corregido en D4.8**: "Auth0 SDK v4.x" → `auth0/login` **v7.x**, y el punto pasa a describir el guard `auth0-api` + `UserRepositoryContract::fromAccessToken()` (§3.2), que es la decisión aprobada.
  - **D2.1**: incorporadas las versiones verificadas del stack, que hasta hoy solo vivían en el handoff de backend.
  - **Aprobado también** que `/api/v1/me` sirva la identidad del **token** y no toque la base de datos en Sprint 1 (§3.4); la provisión JIT queda en HU-04, que es donde nace la tabla `users`.
- `2026-09-17` — **fase Red ejecutada. 22 tests escritos, 22 en rojo** (más 1 guardarraíl verde a propósito). Escribirlos reveló cuatro cosas que la spec aprobada no preveía, y §3.4 y §4 quedan corregidas:
  1. **`setImpersonating()` no pasa por `fromAccessToken()`.** Los tests de extracción de rol se mueven de `tests/Feature/TokenGuardTest.php` a **`tests/Unit/UserRepositoryTest.php`** (es una función pura de los claims), y aparece el test **11c**, que afirma el cableado del repositorio en `config/auth.php`. Sin él, la extracción del rol podría no ejecutarse nunca y nada se pondría rojo.
  2. **`/api/v1/me` no puede devolver `name` ni `email`**: no viajan en un access token de Auth0. El contrato de §3.4 queda en `{ sub, role }`.
  3. **El test 8 se elimina** por nacer verde (C2); su intención se absorbe en el test 1, que ahora afirma el conjunto exacto de claves.
  4. **El test 10 nace verde y se documenta como la única excepción** de la suite: es un guardarraíl, no un criterio de aceptación.
  - Además, `tests/Pest.php` pasa a `->in('Feature', 'Unit')` (los tests de Unit leen `config()`), gana los helpers `impersonarToken()` y `claimsDeToken()`, y pierde el `function something()` y el `expect()->toBeOne()` del esqueleto de Laravel.
  - **Pendiente de la fase Green:** ninguna de las cuatro correcciones toca `bootstrap/app.php`, así que **TS-27 se puede implementar sin instalar Auth0**.
